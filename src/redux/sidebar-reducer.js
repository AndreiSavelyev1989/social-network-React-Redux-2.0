const initialState = {
    friends: [
      { id: 1, name: "Andrei" },
      { id: 2, name: "Dimych" },
      { id: 3, name: "Anastasia" },
    ],
  };

const sidebarReducer = (state = initialState, action) => {
    return state;
}

export default sidebarReducer;